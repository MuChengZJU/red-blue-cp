# Changelog

本项目版本遵循语义化版本。日期为发布日。

## 0.5.1 — 2026-06-07

### 修复
- **旧批次条目点不进详情、没有标题**（0.5.0 用户实测）：0.5.0 之前批量下载的条目没有对应任务记录。现在打开数据库时自动给历史已完成/失败条目回填任务并关联（标题缺失时用 Markdown 文件名兜底），批次卡片里的旧条目从此有标题、可进详情。
- 重新导入清单时，已存在条目的标题会刷新（旧条目当年没存标题）。
- 连带修复去重盲区：升级前批量下载过的内容，现在重新提交/导入也能被识别为「已下过」。

## 0.5.0 — 2026-06-07

**主题：WebUI v2（M5b）——单条/批量整合、批次进任务列表、去重检测。**

### 新增
- **主页两标签**：「单条 / 批量」整合进主页，独立 `/batches` 页移除（301 回主页）。批量导入交互重做：文件选择按钮化、粘贴折叠、批次名称（可选，默认按博主+条数自动取名）、「已下过的也重新下载」「允许半份清单」选项。
- **批次进任务列表**：每批一张卡片（标题 + 进度条 + 成功/失败/跳过/待下计数），默认显示前 5 条可滚动，可展开全部；条目可点进任务详情。
- **批量逐条建任务**：批量下载的每条笔记现在也是一条任务（详情页、用量账单、原地重试全部可用）；批次重跑复用同一条任务原地重置，不堆历史。
- **去重检测**：单条提交已成功下过的内容（链接参数不同也认得出）→ 弹窗「已下过，是否重下」，确认才重下；批量导入默认跳过已下过的并报数，可勾选强制全量。
- `rbcp serve` 新增 `--port`（原写死 8000）。

### 修复
- WebUI 单条/重试管道现在遵循 `RBCP_PROXY`：批量产生的任务在页面点重试不再绕过代理。

### 说明
- 旧库自动迁移（batch 表加 title、batch_item 加 title/job_id），旧批次无名字、条目无详情链接，新批次起生效。

## 0.4.1 — 2026-06-06

**主题：流式修长文超时（M5a）+ 任务用量/费用统计（P1h）。**

### 修复
- **长视频清洗不再超时**：`llm_clean` / `vlm` 改流式 SSE 调用。原来非流式 + 180 秒读超时，长文生成约 300 秒必撞超时（表现为"短视频成功、长视频失败"，且被网络重试放大等待 3 倍）。流式后读超时只需覆盖 token 间隔。
- 流式建连可重试，流中断不重试；响应用完即关，不漏连接。
- 重试任务时清掉上一轮的用量账单，不重复计账。

### 新增
- **任务用量/费用统计**：每个任务记录 ASR 音频秒数、VLM/LLM 输入输出 token 数、各阶段耗时，按 DashScope 官方目录价估算费用。详情页「用量与费用」账单 + 任务列表累计估算费用 + `GET /api/stats`。
- jobs 表加 `usage` 列（JSON），旧库自动迁移；估算为目录价，不含免费额度抵扣。

## 0.4.0 — 2026-06-06

**主题：博主安全批量（M4）+ 错误处理地基 + 一批输入/报错 UX 修复。**

### 新增
- **博主安全批量下载**：MV3 浏览器插件抓小红书博主清单（`user_posted`，去重、滚到底导出 `notes.json`，popup 手动导出/复制到剪贴板）+ `rbcp batch` 走代理批量下载（schema 校验、开跑前出口探测、断点续传、token 过期跳过、失败不中断汇总）。
- **WebUI 批量导入页** `/batches`：上传或粘贴 `notes.json` → 后台批量下载 + 进度轮询。
- **结构化错误体系** `service/errors.py`：异常分层（网络/API/风控/认证/解析等）+ `format_error_for_user` 人话提示；service 层补 logging + 打 response body。
- **代理支持**：`--proxy` / `RBCP_PROXY` 显式穿透到主站下载层（护 IP）。

### 修复 / 改进
- **报错不再泄漏**：失败任务展示人话；技术详情为脱敏异常链摘要，**完整 traceback 只进服务器日志**，不暴露文件路径/用户名。
- **输入更宽容**：单篇输入框接受整段分享文案（带标题/中文尾/emoji），自动抽 URL + 去追踪参数（B 站去 `share_source`/`vd_source` 保 `p`/`t`；小红书保 `xsec_token`/`xsec_source`）。
- **重试原地**：重试复用同一条任务（不再新建），历史不堆积。
- **DashScope 调用**：超时/连接错误重试 3 次；`run` 退出码 bug 修复。
- 小红书 token 过期可靠识别（`/404` + `error_code=300031`）；TUN/系统代理下出口探测不再误判。

### 说明
- 模型仍为 DashScope（ASR `paraformer-v2` / VLM `qwen3-vl-flash` / LLM `qwen-plus`），LLM/VLM 走 OpenAI 兼容端点。
- 红线：抓清单主路径为浏览器插件（pydoll 降为可选）；不打包 chromium；媒体不进知识库。

## 0.3.0
形态落地：自部署 WebUI（红蓝品牌、手机可达）+ PyPI 发布 + CI/CD + 详情页 Markdown 渲染。

## 0.2.0 / 0.1.0
P0：URL → Markdown 闭环（B 站 / 小红书，视频转录 + 图文识别 + 说话人分离），CLI + WebUI 双入口。

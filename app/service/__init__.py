"""Deprecated package — code moved to ``app.extract`` in 0.6.

This shim re-exports the old ``app.service.*`` import paths so that code
written against 0.5.x keeps working. It will be removed in 0.7.

Prefer importing from ``app.extract`` directly.
"""

.env
_reference/
__pycache__/
*.pyc
.venv/
dist/
*.egg-info/
build/
.DS_Store
.pytest_cache/
uv.lock
.gstack/
.leak-patterns.local
.claude/*
!.claude/commands/

# 实验/临时（用完即弃，不进仓库）
_sandbox/
_tmp/

# 知识库索引 DB（红线#4：只该落在 ~/transcript，从仓库目录起 serve 会误写到这）
_index.sqlite
**/_index.sqlite

# PyInstaller 打包 spec（spike/实验产物，按需重生成）
rbcp-serve-*.spec
